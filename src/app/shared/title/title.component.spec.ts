import { TitleComponent } from './title.component';

describe('TitleComponent', () => {
  let component: TitleComponent = new TitleComponent();

  beforeEach(() => {
    component = new TitleComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
